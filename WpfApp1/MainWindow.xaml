<Window x:Name="MyWindow"
        x:Class="WpfLabs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
        mc:Ignorable="d"
        Title="Lab_1_"
        Height="600"
        Width="600"
        MaxHeight="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}}"
        MaxWidth="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}}"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="Navy"
        Cursor="None"
        MouseEnter="MainWindow_OnMouseEnter"
        MouseLeave="MainWindow_OnMouseLeave"
        SizeChanged="MainWindow_OnSizeChanged">
    <Grid x:Name="MyGrid" KeyDown="OnKeyDown">
        <glWpfControl:GLWpfControl
            x:Name="OpenTkControl"
            Ready="OpenTkControl_OnReady"
            Render="OpenTkControl_OnRender" />
        <TextBlock x:Name="MyTextBlock" />
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Отрисовка">
                    <MenuItem x:Name="Object" Header="Объект" Click="OnClickColor" />
                    <MenuItem x:Name="Skeleton" Header="Каркас" Click="OnClickColor" />
                    <MenuItem x:Name="Normals" Header="Нормали" Click="OnClickColor" />
                    <MenuItem x:Name="Textures" Header="Текстуры" Click="OnClickColor" />
                </MenuItem>
                <MenuItem Header="Проэкция">
                    <MenuItem x:Name="Orthogonal" Header="Ортогональная" Click="OnClickColor" />
                    <MenuItem x:Name="Perspective" Header="Перспективная " Click="OnClickColor" />
                    
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>